export enum GameState {
  IDLE,
  ACTIVE,
  FINISHED,
}

export enum Color {
  RED,
  YELLOW,
  BLUE,
  GREEN,
  JOKER,
}

export enum CardType {
  NUMBER,
  DRAW2,
  REVERSE,
  SKIP,
  WILD,
  WILD4,
}
